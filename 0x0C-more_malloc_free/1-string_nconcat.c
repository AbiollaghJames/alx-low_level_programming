#include "main.h"
#include <stdlib.h>
#include <stdio.h>
/**
 * string_nconcat - concatenates two strings
 * @s1: par 1
 * @s2: par 2
 * @n: par 3
 * Return: 0 Success
 */
char *string_nconcat(char *s1, char *s2, unsigned int n)
{
  char *str;
  int i = 0, j = 0, len  =0, len1 = 0;

  while (s1 && s1[len])
    len++;
  while (s2 && s2[len1]
	 len1++;

	 if (n < len1)
	   stt = malloc(sizeof(char) * (len + n +1));
	 else
	   str = m9alloc(sizeo(char) * ( len + len1 + 1));

	 if (!str)
	   return (NULL);

	 while (i < len)
	   {
	   s[i] = s1[i];
	   i++;
	   }
}
