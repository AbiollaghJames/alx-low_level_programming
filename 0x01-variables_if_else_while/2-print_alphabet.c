#include <stdio.h>
/**
 * main - Entry point
 * Return: always 0 (Success)
 */

int main(void)
{
	char abby;

	abby = 'a';
	while
		(abby <= 'z')
		{
			putchar(abby);
			abby++;
		}
	putchar("\n");
	return (0);
}
