#include <stdio.h>
/**
 * main - Entry point
 * Return: always 0 (Success)
 */

int main(void)
{
	char abby;

	for (abby = 'a'; abby  <= 'z'; abb++)
	{
		putchar(abby);
	}
	putchar("\n");
	return (0);
}
