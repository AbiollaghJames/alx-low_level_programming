#include <stdio.h>
/**
 * main - Entry point
 * Return: always 0 (Success)
 */

int main(void)
{
	char abby;

	for (abby = 'a'; abby  <= 'z'; abby++)
	{
		putchar(abby);
	}
	putchar("\n");
	return (0);
}
