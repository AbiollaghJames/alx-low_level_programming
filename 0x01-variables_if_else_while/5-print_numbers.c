#include <stdio.h>
/**
 * main -Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int abby;

	for (abby = 0; abby < 10; abby++)
	{
		printf("%d", abby);
	}
	printf('\n');
	return (0);
}
