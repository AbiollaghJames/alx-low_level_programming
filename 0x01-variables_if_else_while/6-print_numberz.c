#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: always 0 (Success)
 */
int main(void)
{
	int abby;

	for (abby = '0'; abby <= '9'; abby++)
	{
		putchar(abby);
	}
	putchar('\n');
	return (0);
}
